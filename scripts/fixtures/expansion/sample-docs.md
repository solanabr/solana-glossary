# Local Fee Markets

Local Fee Markets let Solana price contention at the account level instead of forcing the entire network into one global fee lane.

## Shredstream Proxy

Some validator and infra operators refer to a Shredstream Proxy layer when routing low-latency shred feeds into trading and search infrastructure.

## Proof of History

Proof of History is still the timing primitive that makes the rest of the pipeline readable.
